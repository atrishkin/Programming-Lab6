import javax.swing.*;

public class SaveButton{
    //TODO: Кнопка сохранения в файл
    JButton SaveButton(){
        JButton saveButton = new JButton("Сохранить");
        return saveButton;
    }
}
